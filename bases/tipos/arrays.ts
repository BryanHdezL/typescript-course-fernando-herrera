;(() => {
  //   const numbers: (string | number | boolean)[] = [1, 2, '3', '4', true, flase]
  const numbers: number[] = [1, 2, 3, 4, 5]
  const villians: string[] = ['Omega Rojo', 'Dormamu', 'Duente Verde']

  console.log(numbers)
  villians.forEach(v => console.log(v.toUpperCase()))
})()
